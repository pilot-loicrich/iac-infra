name: CI/CD Pipeline - Build, Test, Push, Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: Lint YAML et Terraform
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Installation yamllint
        run: pip install yamllint
      - name: Lint YAML
        run: yamllint ansible/
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.14.8
      - name: Terraform validate
        run: |
          cd terraform
          terraform init
          terraform validate

  build-and-push:
    name: Build et Push image Docker
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v3
      - name: Login Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build image Nginx custom
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/iac-nginx:latest -f docker/Dockerfile .
      - name: Push image sur Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/iac-nginx:latest

  deploy:
    name: Deploy sur Kubernetes
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - uses: actions/checkout@v3
      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
      - name: Valider les manifests Kubernetes
        run: |
          echo "Validation des manifests Kubernetes (dry-run sans cluster)"
          kubectl apply --dry-run=client --validate=false -f kubernetes/nginx.yml
          kubectl apply --dry-run=client --validate=false -f kubernetes/postgres.yml
          kubectl apply --dry-run=client --validate=false -f kubernetes/hpa.yml
          echo "Tous les manifests sont valides - prets pour deploiement"
